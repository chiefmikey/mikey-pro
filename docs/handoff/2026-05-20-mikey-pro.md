# Handoff — mikey-pro rollout + tooling cleanup

_Date: 2026-05-20_
_Status: COMPLETE (one deferred item: depsweep)_

## What this session did

Two related pushes across the user's ~37 owned repos in `~/Nextcloud/mikmac/dev` (NOT the mikey-pro repo itself — it was untouched):

1. **Finished the mikey-pro guardrails rollout** — every repo's `chore/adopt-mikey-pro` branch ff-merged to main, repo left clean on main, no lost work. Phase C repos with real violations finalized by Sonnet workers (tests green before merge): donnie@4a60f17, brave-tab-focus-fix@030078c, copilot-eslint-security@856536e (108 violations fixed), eslint-plugin-disable-autofix@02083e1. Pilot cronparse@f8bac58 merged; hey's stale branch deleted.

2. **Stripped redundant lint/format tooling** that mikey-pro bundles transitively (`@typescript-eslint/*`, `typescript-eslint` meta-pkg, `@eslint/js`, `eslint-import-resolver-typescript`, `eslint-formatter-compact`, `eslint-config-prettier`) from 17 repos. Removed leftover legacy `.eslintrc*`. Fixed 4 CJS repos that had BOTH `eslint.config.js` and `.mjs` (kept `.mjs` — ESM-syntax `.js` is invalid in a CommonJS package). Converted stale `eslint --ext` scripts to flat-config `eslint .`. Gate per repo: `npx eslint .` loads config and `@typescript-eslint` rules fire (proves mikey-pro's bundled copy resolves).

## Loose ends

- **depsweep — DEFERRED.** Guardrails committed (eb1bd1a) and active on its `chore/adopt-mikey-pro` branch, but ~75 files of unrelated live dev (CI/lint-cleanup) sit uncommitted there. When that work lands: ff-merge eb1bd1a to main AND strip its redundant lint deps (`@eslint/js`, `typescript-eslint`).
- **hey** — left dirty intentionally (live bridge/stanley dev). ruff.toml guardrail committed (01c9c02); the 10 dirty files are the user's active work, not ours.

## Kept by design (do NOT strip — lint deps are their product/test surface)

- `eslint-config-react-app-bump` (published ESLint config; plugins are its `dependencies`)
- `copilot-eslint-security` (`@typescript-eslint/utils` runtime + `rule-tester`)
- `eslint-plugin-disable-autofix` (self-ref + `eslint-plugin-unicorn` test fixtures)

## Context that would be lost

Full detail in memory: `project_mikey_pro_rollout_complete.md` (auto-loads). Key gotchas captured there: PyPI `mikey-pro-ruff-config` never published (404) → Python projects use VENDORED ruff.toml; `git add -- f1 f2 missing` aborts ALL staging; `git add -A` sweeps untracked node_modules; clipharvest ruff.toml is hand-tuned (do not overwrite); mattermost-mobile + markd skipped.
